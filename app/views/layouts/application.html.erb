<!DOCTYPE html>
<html>
  <head>
    <title>Flownote v2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/holiday.css@0.9.8" />
    <link rel="stylesheet" href="https://unpkg.com/dracula-prism/dist/css/dracula-prism.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/prismjs@1.24.1/components/prism-core.min.js" %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/prismjs@1.24.1/plugins/autoloader/prism-autoloader.min.js" %>
  </head>

  <body>
    <% if notice %>
      <p style="color: green"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p style="color: red"><%= alert %></p>
    <% end %>
      <nav class="main">
        <ul>
          <div>
            <li style="display: flex; align-content: center; font-size: 1.2rem">Flownote <kbd style="font-size: .9rem; margin-left: 0.4rem;">v2</kbd></li>
          </div>
          <% if user_signed_in? %>
            <div>
              <li><%= link_to "🗒 Notes", notes_path, style: "margin-right: 0.4rem" %></li>
              <li><%= link_to "🔍 Search", search_path, style: "margin-right: 0.4rem" %></li>
              <li><%= link_to "🛠 Account", edit_user_registration_path(current_user), style: "margin-right: 0.4rem" %></li>
              <li><%= link_to '🚪 Logout', destroy_user_session_path, data: { "turbo-method": :delete } %></li>
            </div>
          <% elsif @share_user %>
            <div>
              <li><%= link_to "~#{@share_user.username}'s public notes", shares_path(user: @share_user.username) %></li>
            </div>
          <% end %>
        </ul>
      </nav>
    <main>
      <%= yield %>
    </main>
    <footer>
      Rendered: <%= time_tag Time.now %>
    </footer>
  </body>
</html>
