<div id="<%= dom_id note %>">
  <p>
    <h1><%= note.title %></h1>
  </p>

  <p>Updated: <%= time_tag note.updated_at, time_ago(note.updated_at) %></p>

  <article>
    <%= note.to_html %>
  </article>

</div>
