<nav>
  <ul>
    <li><%= link_to "Rename", edit_folder_path(@folder) %></li>
    <li><%= link_to "All folders", folders_path %></li>
    <li><%= button_to "Delete", @folder, style: "background-color: red", method: :delete, form: { data: { 'turbo-confirm': 'Are you sure you want to delete this folder and all of its files?' } } %></li>
  </ul>
</nav>

<%= render "files_form", folder: @folder %>

<%= render @folder %>

